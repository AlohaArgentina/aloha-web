/* Clases compartidas de formularios en los paneles internos (área clientes /
   admin). Mismos estilos que ya usa el login de /area-clientes, para no
   tener dos sistemas de inputs conviviendo en el mismo producto. */

export const claseEtiqueta = "block text-sm font-medium text-foreground mb-1.5";

export const claseCampo =
  "w-full px-4 py-2.5 rounded-lg border border-border bg-card text-foreground placeholder:text-muted-foreground " +
  "focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 focus:ring-offset-background transition";

export const claseAyuda = "text-xs text-muted-foreground mt-1.5";
